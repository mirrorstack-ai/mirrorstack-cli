# Module-scoped SQL

Place migrations here only for data shared across every installation of this
module. App-owned data belongs in `../app`. Add a Module migration version in
`main.go` when the first shared migration is introduced.
