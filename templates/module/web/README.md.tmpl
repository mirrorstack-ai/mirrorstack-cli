# Optional web surface

Create this package only when the module declares UI. Consume mount contracts
from `@mirrorstack-ai/app-module-client/web` and use the workspace web build
scripts instead of module-local build configuration.
