-- App-scoped data is isolated per installed application.
CREATE TABLE IF NOT EXISTS __MS_MODULE_ID___items (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    name text NOT NULL,
    created_at timestamptz NOT NULL DEFAULT now()
);
