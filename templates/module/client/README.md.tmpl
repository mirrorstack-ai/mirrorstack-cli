# Optional client package

Create this package only when the module exposes a reusable browser API. Build
on `@mirrorstack-ai/app-module-client`, keep plugin options and module API
types separate, and use the workspace client build check instead of copying it.
