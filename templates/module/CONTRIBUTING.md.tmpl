# Contributing

Keep declarations in `manifest/`, transport code in `internal/transport/`,
and durable schema changes in the correct `sql/app` or `sql/module` scope.
Run the repository checks before publishing a version.
